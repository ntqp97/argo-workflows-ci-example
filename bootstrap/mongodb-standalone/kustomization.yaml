apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []

helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: mongodb-standalone
    releaseName: mongodb-standalone
    namespace: mongodb-standalone
    valuesFile: ../../charts/mongodb-standalone/values.yaml
